<div class="wrapper">
  <div class="container">
    <div id='schedule-new-btn' class='btn'>
      <%= link_to "新規作成",new_schedule_path%>
    </div>
    <div>
      <%= form_with url:'#', local: true do |f| %>
        <%= f.text_field :title %>
      <% end %>
      <div class="btn-group">
        <%= link_to "編集",'#'%>
        <%= link_to "印刷",'#'%>
      </div>
    </div>
  </div>
</div>